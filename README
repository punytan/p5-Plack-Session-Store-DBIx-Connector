This is Perl module Plack::Session::Store::DBIx::Connector.

INSTALLATION

Plack::Session::Store::DBIx::Connector installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpanm Plack::Session::Store::DBIx::Connector

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Plack::Session::Store::DBIx::Connector documentation is available as in POD. So you can do:

    % perldoc Plack::Session::Store::DBIx::Connector

to read the documentation online with your favorite pager.

punytan
